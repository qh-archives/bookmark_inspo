[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake", "nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = [
  "npm install",
  "cd ../client && npm install && npm run build"
]

[start]
cmd = "node index.js"
